services:
  postgres:
    image: 'postgres:11'
    environment:
      - 'POSTGRES_DB=ed-db-archetype-testcontainers'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=app-user'
    ports:
      - '5432'
